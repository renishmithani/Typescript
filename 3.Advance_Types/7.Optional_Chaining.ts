const fetching_users = {
    id: 1,
    name: "Person",
    // job: { title: "CEO", description: "My Own Company" },
};

console.log(fetching_users?.job?.title); //Optional Chaining //it means the properties is true then access its value
